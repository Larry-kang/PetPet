@model PetPet.Domain.Entities.News

@{
    ViewData["Title"] = Model.Title;
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-9">
            <div class="card glass-card border-0">
                <div class="card-body p-5">
                    <nav aria-label="breadcrumb" class="mb-4">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a asp-action="Index">最新消息</a></li>
                            <li class="breadcrumb-item active" aria-current="page">公告詳情</li>
                        </ol>
                    </nav>

                    <h1 class="fw-bold text-primary mb-3">@Model.Title</h1>
                    <p class="text-muted border-bottom pb-3">
                        📅 發佈日期：@Model.PublishedAt.ToString("yyyy年MM月dd日 HH:mm")
                    </p>

                    <div class="mt-4 lh-lg fs-5">
                        @Html.Raw(Model.Content.Replace("\n", "<br>"))
                    </div>

                    <div class="mt-5 text-center">
                        <a asp-action="Index" class="btn btn-outline-primary rounded-pill px-4">返回列表</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
