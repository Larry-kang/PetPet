@model IEnumerable<PetPet.Domain.Entities.News>

@{
    ViewData["Title"] = "最新消息";
}

<div class="row mb-5 text-center">
    <div class="col-12">
        <h1 class="display-5 fw-bold text-primary">📰 最新消息</h1>
        <p class="lead text-muted">掌握 PetPet 第一手公告與活動</p>
    </div>
</div>

<div class="row">
    <div class="col-md-10 mx-auto">
        <div class="list-group shadow-sm rounded-4 overflow-hidden">
            @foreach (var item in Model)
            {
                <a asp-action="Details" asp-route-id="@item.Id" class="list-group-item list-group-item-action p-4 border-start-0 border-end-0">
                    <div class="d-flex w-100 justify-content-between align-items-center mb-2">
                        <h4 class="mb-1 fw-bold text-dark">@item.Title</h4>
                        <small class="text-muted">@item.PublishedAt.ToString("yyyy/MM/dd")</small>
                    </div>
                    <p class="mb-1 text-secondary text-truncate">@item.Content</p>
                    <small class="text-primary">了解更多 &rarr;</small>
                </a>
            }

            @if (!Model.Any())
            {
                <div class="text-center p-5 bg-white">
                    <span class="display-6 text-muted">📭 目前沒有新公告</span>
                </div>
            }
        </div>
    </div>
</div>
