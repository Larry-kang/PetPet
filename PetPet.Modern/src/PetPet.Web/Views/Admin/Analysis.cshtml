@{
    ViewData["Title"] = "統計分析";
}

<div class="container mt-4">
    <h2 class="mb-4">📊 平台統計分析</h2>

    <div class="row">
        <div class="col-md-6 mb-4">
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    寵物品種分佈
                </div>
                <div class="card-body">
                    <canvas id="varietyChart"></canvas>
                </div>
            </div>
        </div>
        <div class="col-md-6 mb-4">
            <div class="card shadow">
                <div class="card-header bg-info text-white">
                    會員地區分佈
                </div>
                <div class="card-body">
                    <canvas id="cityChart"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // Pet Variety Chart
        var varietyCtx = document.getElementById('varietyChart').getContext('2d');
        var varietyChart = new Chart(varietyCtx, {
            type: 'doughnut',
            data: {
                labels: @Html.Raw(Json.Serialize(ViewBag.VarietyLabels)),
                datasets: [{
                    data: @Html.Raw(Json.Serialize(ViewBag.VarietyData)),
                    backgroundColor: [
                        '#FF6384', '#36A2EB', '#FFCE56', '#4BC0C0', '#9966FF', '#FF9F40'
                    ]
                }]
            }
        });

        // City Chart
        var cityCtx = document.getElementById('cityChart').getContext('2d');
        var cityChart = new Chart(cityCtx, {
            type: 'bar',
            data: {
                labels: ['Taipei', 'Taichung', 'Kaohsiung', 'Tainan', 'Hsinchu'], // Hardcoded based on Seeder CityId logic for demo
                datasets: [{
                    label: '會員人數',
                    data: @Html.Raw(Json.Serialize(ViewBag.CityData)),
                    backgroundColor: '#36A2EB'
                }]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });
    </script>
}
